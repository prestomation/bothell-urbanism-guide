name: PR Preview Cleanup

on:
  pull_request:
    types: [closed]

jobs:
  cleanup:
    uses: prestomation/urbanism-guide-core/.github/workflows/pr-preview-cleanup.yml@v0.1.0
    permissions:
      contents: write
